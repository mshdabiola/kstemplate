ktor:
    application:
        modules:
            - com.mshdabiola.kstemplate.ApplicationKt.module
    deployment:
        port: 8080

    development: false
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
postgres:
  url: $?POSTGRES_URL
  user: $?USER_DATABASE
  password: $?PASSWORD_DATABASE